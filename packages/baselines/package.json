{
  "name": "@repo/baselines",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Envelope-typed read/write/compare/format primitives for the seven per-workspace quality baselines (lint, coverage, crap, maintainability, mutation, lighthouse, bundle-size). AJV-validated against the schemas in .agents/schemas/baselines/.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "lint": "pnpm exec biome check . && pnpm exec eslint .",
    "typecheck": "pnpm exec tsc --noEmit -p tsconfig.json",
    "test": "pnpm exec vitest run",
    "build": "exit 0"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "@types/node": "^25.8.0",
    "vitest": "^4.1.6"
  }
}
