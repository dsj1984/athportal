/**
 * Form interaction step library.
 *
 * Reserved for downstream Epics. Will own When phrases for filling fields,
 * submitting forms, and asserting validation feedback as forms accrete in
 * the web client (sign-up, onboarding, team-creation, etc.).
 *
 * Step authoring rules — no DOM selectors, no `data-testid` values in
 * scenarios, no raw URLs — live in `.agents/rules/gherkin-standards.md`
 * and `docs/testing-strategy.md`.
 */
export {};
