---
// apps/web/src/components/ui/Avatar.astro
//
// Shared Avatar primitive. Renders the user's uploaded photo when
// `src` is provided, otherwise renders the uppercase-initials
// fallback over an OKLCH brand gradient keyed off `hue`. PRD #703 /
// Tech Spec #704; refactored under Story #713 / Task #721 from the
// prior Story #574 / Task #585 implementation.

import { type AvatarProps, buildAvatarView } from './Avatar.ts';

type Props = AvatarProps;
const view = buildAvatarView(Astro.props);
---

{view.src !== null ? (
  <img
    class="avatar avatar--image"
    src={view.src}
    alt={view.name}
    data-testid={view.testId}
    style={view.imageStyle}
  />
) : (
  <span
    class="avatar avatar--initials"
    role="img"
    aria-label={view.name}
    data-testid={view.testId}
    style={view.fallbackStyle}
  >
    {view.initials}
  </span>
)}
