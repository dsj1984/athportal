// apps/api/src/routes/v1/admin/org.ts
//
// Placeholder router for `/api/v1/admin/org` (Story #654, Task #658).
// Story #655 (Epic #10) replaces this file with the real organization
// admin handlers — the mount point in `./index.ts` stays the same so
// downstream Stories do not re-edit the API entrypoint.

import { placeholderRouter } from './_placeholder';

export const orgAdminRoute = placeholderRouter('organization');
